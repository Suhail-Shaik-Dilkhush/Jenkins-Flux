---

apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: frontend
spec:
  selector:
      app: frontend-pod
  type: NodePort
  ports:
    - port: 15000
      targetPort: 15000